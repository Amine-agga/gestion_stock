# ==================== APPLICATION ====================
spring.application.name=ms-commande

# ==================== SERVER ====================
server.port=9003

# ==================== CONFIG SERVER ====================
spring.config.import=optional:configserver:http://localhost:8888
spring.cloud.config.enabled=true
spring.cloud.config.fail-fast=false
spring.cloud.config.uri=http://localhost:8888

# ==================== DATABASE ====================
spring.datasource.url=jdbc:postgresql://localhost:5437/commande_db
spring.datasource.username=admin
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# ==================== JPA / HIBERNATE ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==================== EUREKA CLIENT ====================
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# ==================== FEIGN CLIENT ====================
# ? IMPORTANT : Activer Apache HttpClient pour supporter PATCH
feign.httpclient.enabled=true

# Configuration Feign
feign.client.config.default.connect-timeout=5000
feign.client.config.default.read-timeout=5000
feign.circuitbreaker.enabled=true

# ==================== EMAIL CONFIGURATION ====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=stockflowsf@gmail.com
spring.mail.password=fruc yugz ccjk vxlc

# ? PROPRIÉTÉS SMTP ESSENTIELLES (MANQUANTES DANS TON CONFIG)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ? CONFIGURATION POUR LA VALIDATION PAR EMAIL
# URL de base pour les liens de validation (à adapter selon ton environnement)
app.base.url=https://2d8221398ded.ngrok-free.app
# Email de l'administrateur pour recevoir les notifications
app.admin.email=stockflowsf@gmail.com

# ==================== COMPATIBILITY ====================
# Désactiver la vérification de compatibilité
spring.cloud.compatibility-verifier.enabled=false

# Forcer le démarrage même avec compatibilité
spring.main.allow-bean-definition-overriding=true